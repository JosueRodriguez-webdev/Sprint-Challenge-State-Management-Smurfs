export let initialState = {
  smurfs: [
    {
      name: "Brainey",
      age: 200,
      height: "5cm",
      id: 0
    }
  ]
};

export const smurfsReducer = (actions = initialState, actions) => {
  switch (actions.type) {
    default:
      return state;
  }
};
