export let initialState = {
  smurfs: [],
  isPosting: false,
  isFetching: false,
  errors: ""
};

export const smurfsReducer = (state = initialState, actions) => {
  switch (actions.type) {
    default:
      return state;
  }
};
